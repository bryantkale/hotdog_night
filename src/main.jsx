import React from "react";
import ReactDOM from "react-dom/client";
import HotDogLog from "../HotDogLog";

ReactDOM.createRoot(document.getElementById("root")).render(
    <React.StrictMode>
        <HotDogLog />
    </React.StrictMode>
);
